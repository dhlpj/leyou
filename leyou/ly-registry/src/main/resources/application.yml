server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false #是否将自己注册到eureka Server 默认为true
    fetch-registry: false #是否从其他eureka Server上获取注册信息 默认为true